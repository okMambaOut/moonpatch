name = "local/moonpatch"

version = "0.1.0"

readme = "README.md"

repository = ""

license = "Apache-2.0"

keywords = [ "unified-diff", "patch", "source-text", "developer-tools" ]

preferred_target = "wasm-gc"

description = "Deterministic unified-diff parsing and transactional patch application for MoonBit"

import {
  "moonbitlang/x@0.4.49",
}
